# dependencies
node_modules/

# build output
dist/
dist-ssr/

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# environment / secrets
.env
.env.*
!.env.example

# temporary files
*.tmp
*.temp
*~

# editor / OS / local tooling
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
*.local
.claude/

# test / coverage
coverage/

# TypeScript incremental build cache
*.tsbuildinfo

# never commit real, unsanitised agent logs (only the sanitised export under planning/agent-logs/ is allowed in)
*.raw.jsonl
planning/agent-logs/raw/
